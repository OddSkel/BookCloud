name: CI - Dev Branch

on:
  push:
    branches:
      - dev

jobs:
  rust-unit-tests:
    name: Rust services - build and unit tests
    runs-on: ubuntu-latest

    strategy:
      matrix:
        package:
          - api-gateway
          - book-catalog
          - book-search
          - rating-catalog
          - author-analytics-service
          - book-recommendation

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Rust service
        run: cargo build -p ${{ matrix.package }} --verbose

      - name: Run Rust unit tests
        run: cargo test -p ${{ matrix.package }} --verbose

  python-unit-tests:
    name: Python services - unit tests
    runs-on: ubuntu-latest

    strategy:
      matrix:
        service:
          - src/services/genre-analysis-service
          - src/services/compare-service

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        working-directory: ${{ matrix.service }}
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest

      - name: Run Python unit tests
        working-directory: ${{ matrix.service }}
        run: pytest -v
