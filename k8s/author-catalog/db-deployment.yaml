apiVersion: apps/v1
kind: Deployment
metadata:
  name: author-db
  namespace: bookcloud
  labels:
    app: author-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: author-db
  template:
    metadata:
      labels:
        app: author-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - name: postgres
              containerPort: 5432
          envFrom:
            - configMapRef:
                name: author-db-config
            - secretRef:
                name: postgres-credentials
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: "100m"
            limits:
              cpu: "300m"
          readinessProbe:
            tcpSocket:
              port: postgres
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: postgres
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: author-db-data
