apiVersion: apps/v1
kind: Deployment
metadata:
  name: author-catalog
  namespace: bookcloud
  labels:
    app: author-catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: author-catalog
  template:
    metadata:
      labels:
        app: author-catalog
    spec:
      containers:
        - name: author-catalog
          image: bookcloud/author-catalog:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50052
          envFrom:
            - configMapRef:
                name: author-catalog-config
            - secretRef:
                name: postgres-credentials
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "400m"
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 15
            periodSeconds: 20
