apiVersion: v1
kind: Service
metadata:
  name: author-catalog
  namespace: bookcloud
spec:
  selector:
    app: author-catalog
  ports:
    - name: grpc
      port: 50052
      targetPort: grpc
  type: ClusterIP
