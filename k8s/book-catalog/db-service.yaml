apiVersion: v1
kind: Service
metadata:
  name: book-db
  namespace: bookcloud
spec:
  selector:
    app: book-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  type: ClusterIP
