apiVersion: v1
kind: Service
metadata:
  name: book-catalog
  namespace: bookcloud
spec:
  selector:
    app: book-catalog
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
  type: ClusterIP
