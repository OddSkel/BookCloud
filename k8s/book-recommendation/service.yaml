apiVersion: v1
kind: Service
metadata:
  name: book-recommendation
  namespace: bookcloud
  labels:
    app: book-recommendation
spec:
  type: ClusterIP
  selector:
    app: book-recommendation
  ports:
    - name: grpc
      port: 50056
      targetPort: 50056
