apiVersion: v1
kind: Service
metadata:
  name: compare-redis
  namespace: bookcloud
  labels:
    app: compare-redis
spec:
  type: ClusterIP
  selector:
    app: compare-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis