apiVersion: v1
kind: Service
metadata:
  name: genre-db
  namespace: bookcloud
spec:
  selector:
    app: genre-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  type: ClusterIP
