apiVersion: v1
kind: Service
metadata:
  name: genre-analysis-service
  namespace: bookcloud
spec:
  selector:
    app: genre-analysis-service
  ports:
    - name: grpc
      port: 50055
      targetPort: grpc
  type: ClusterIP
