apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rating-catalog
  namespace: bookcloud
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: rating-catalog
  namespaceSelector:
    matchNames:
      - bookcloud
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
