apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-catalog
  namespace: bookcloud
  labels:
    app: rating-catalog
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rating-catalog
  template:
    metadata:
      labels:
        app: rating-catalog
    spec:
      containers:
        - name: rating-catalog
          image: bookcloud/rating-catalog:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50053
            - name: metrics
              containerPort: 9100
          envFrom:
            - configMapRef:
                name: rating-catalog-config
            - secretRef:
                name: postgres-credentials
          resources:
            requests:
              cpu: "100m"
              memory: "96Mi"
            limits:
              cpu: "300m"
              memory: "192Mi"
          readinessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 10
          startupProbe:
            tcpSocket:
              port: grpc
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 15
            periodSeconds: 20
