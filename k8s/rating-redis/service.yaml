apiVersion: v1
kind: Service
metadata:
  name: rating-redis
  namespace: bookcloud
  labels:
    app: rating-redis
spec:
  type: ClusterIP
  selector:
    app: rating-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
