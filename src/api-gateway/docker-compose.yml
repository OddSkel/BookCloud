services:
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HOST_API_PORT}:${API_PORT}"
    networks:
      - bookcloud-network

networks:
  bookcloud-network:
    external: true
