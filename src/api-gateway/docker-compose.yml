services:
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HOST_API_PORT}:8080"
    environment:
      RUST_LOG: info
    networks:
      - bookcloud-network

networks:
  bookcloud-network:
    external: true

