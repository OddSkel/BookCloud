services:
  compare-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: compare-service
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HOST_GRPC_PORT}:${GRPC_PORT}"
    networks:
      - bookcloud-network

networks:
  bookcloud-network:
    external: true
